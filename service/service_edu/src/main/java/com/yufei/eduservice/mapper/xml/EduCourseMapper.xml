<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yufei.eduservice.mapper.EduCourseMapper">
    <select id="getPublishCourseInfo" resultType="com.yufei.eduservice.entity.vo.CoursePublishVo">
select ec.id,ec.title,ec.price,ec.cover,ec.lesson_num AS lessonNum,ecd.description,et.name AS teacherName,es1.title AS subjectLevelOne,es2.title AS subjectLevelTwo
from edu_course ec LEFT JOIN edu_course_description ecd on ec.id = ecd.id
left join
edu_teacher et on ec.teacher_id = et.id
LEFT JOIN
edu_subject es1 on ec.subject_parent_id = es1.id
LEFT JOIN edu_subject es2 on ec.subject_id = es2.id
where ec.id = #{courseId}
    </select>
</mapper>
